import { IEntry } from '../store/entry/slice';

const mockShowEntry: IEntry = {
  title: 'Another day in Warsaw',
  note: 'It was a cold and rainy day...',
  status: 'show',
};

export default mockShowEntry;
